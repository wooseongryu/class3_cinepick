<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.itwillbs.cinepick.mapper.AdminMapper">
	<!-- 공지 -->
	<insert id="insertNotice">
		INSERT INTO notice
		VALUES (
			null
			, #{noticeTitle}
			, #{noticeContent}
			, now()
		)
	</insert>
	
	<select id="selectNoticeList" resultType="com.itwillbs.cinepick.vo.NoticeVO">
          SELECT * FROM notice;
    </select>
    
    <select id="selectNotice" resultType="com.itwillbs.cinepick.vo.NoticeVO">
		SELECT
			noticeIdx
			, noticeTitle
			, noticeContent
		FROM 
			notice
		WHERE 
			noticeIdx = #{noticeIdx };
    </select>
    
    <delete id="deleteNotice">
    	DELETE FROM notice
    	WHERE noticeIdx = #{noticeIdx };
    </delete>
    
    <update id="updateNotice">
    	UPDATE 
    		notice
    	SET 
    		noticeTitle = #{noticeTitle }
    		, noticeContent = #{noticeContent }
    	WHERE 
    		noticeIdx = #{noticeIdx };
    </update>
    <!-- 공지 끝 -->
    
    
    <!-- 자주 묻는 질문 카테고리 -->
    <insert id="insertCategory">
		INSERT INTO qnaCate
		VALUES (
			null
			, #{qnaCateSubject}
		)
	</insert>

	<select id="selectCategory" resultType="com.itwillbs.cinepick.vo.QnaCateVO">
          SELECT 
          	qnaCateIdx 
          	, qnaCateSubject
          FROM 
          	qnaCate;
    </select>
    <!-- 자주 묻는 질문 카테고리 끝 -->
    
    
    <!-- 자주 묻는 질문 -->
  	<insert id="insertQna">
		INSERT INTO qna
		VALUES (
			null
			, #{qnaTitle}
			, #{qnaContent}
			, #{qnaCategory}
		)
	</insert>

	<select id="selectQnaList" resultType="com.itwillbs.cinepick.vo.QnaVO">
		SELECT
			q.qnaIdx
			, q.qnaTitle
			, q.qnaContent
			, q.qnaCategory
			, c.qnaCateSubject 
		FROM
			qna q JOIN qnaCate c
			ON q.qnaCategory = c.qnaCateIdx
		<if test="qnaIdx != null and !qnaIdx.equals('')">
		WHERE q.qnaIdx = #{qnaIdx}
		</if>
    </select>
    <!-- 자주 묻는 질문 끝 -->
</mapper>